# roc_desk-ssh standalone build

Run from the repository root:

`powershell
.\build-standalone.ps1
`

The release executable is written to in\roc_desk-ssh.exe. Use
-Configuration debug for a fast development build.
