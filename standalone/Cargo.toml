[package]
name = "roc_desk_ssh_standalone"
version = "0.1.0"
edition = "2021"

[dependencies]
roc_desk_ssh = { path = "../lib" }

[target."cfg(windows)".dependencies]
windows = { version = "0.61", features = ["Win32_UI_WindowsAndMessaging", "Win32_Foundation"] }
